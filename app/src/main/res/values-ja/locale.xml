<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- There doesn't seem to be a way to get the locale that Android chose at runtime if the device locale is not supported.
        All of the below return the system locale:
        Locale.getDefault()
        resources.getConfiguration().getLocales().get(0)
        resources.getConfiguration().locale
     -->
    <string name="current_locale">ja-JP</string>
</resources>

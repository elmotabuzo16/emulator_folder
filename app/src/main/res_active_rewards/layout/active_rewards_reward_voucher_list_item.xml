<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="16dp"
        tools:ignore="ContentDescription"
        tools:src="@drawable/starbucks_reward_sml" />

    <TextView
        android:id="@+id/reward_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/icon"
        android:textAppearance="@style/MaterialLightBody1Secondary"
        tools:text="Starbucks" />

    <TextView
        android:id="@+id/reward_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@id/reward_name"
        android:layout_below="@id/reward_name"
        android:layout_toEndOf="@+id/icon"
        android:textAppearance="@style/MaterialLightSubheadMedium"
        tools:text="$5 Voucher" />

    <TextView
        android:id="@+id/expiry_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@id/reward_name"
        android:layout_below="@+id/reward_description"
        android:layout_marginTop="4dp"
        tools:text="Expires 20 August 2016" />

    <TextView
        android:id="@+id/get_reward_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@id/reward_name"
        android:layout_below="@id/expiry_date"
        android:layout_marginTop="18dp"
        android:visibility="gone"
        android:text="@string/AR.rewards.use_reward_title_720"
        android:textAppearance="@style/TextAppearance.AppCompat.Widget.Button"
        android:textColor="@color/waterBlue" />

</RelativeLayout>
